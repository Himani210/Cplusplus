#include<stdio.h>
#include<conio.h>
void main()
{
   int i,j,n,temp,arr[5];
   clrscr();
{

   for(i=0;i<5;i++)
   {
   //cout<<"Enter your array : ";
   scanf("%d",&arr[5]);
   }
   for(i=0;i<n;i++)
   {
     for(j=0;j<n-i-1;j++)
     {
       if(arr[j]>arr[j+1])
       {
	 temp=arr[j];
	 arr[j]=arr[j+1];
	 arr[j+1]=temp;
       }
       }
       }
   printf("\n Sorted array : %d",a[5]);

}
getch();
}
